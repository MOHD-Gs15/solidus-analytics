name: CodeQL

on:
  push:
    branches: [ main, master, dev ]
  pull_request:
    branches: [ main, master, dev ]
  schedule:
    - cron: '0 6 * * 1'

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a3267 # v4 (SA2-027: SHA-pinned)

      - name: Set up JDK 25
        uses: actions/setup-java@cf277c60eb25 # v4 (SA2-027)
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@faaca9a8f6ed # v3 (SA2-027)
        with:
          languages: java, javascript-typescript
          build-mode: autobuild

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@faaca9a8f6ed # v3 (SA2-027)
        with:
          category: "/language:java"
